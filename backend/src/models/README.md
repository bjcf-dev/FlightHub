# Carpeta `models`

En esta carpeta se almacenarán los modelos de datos de la aplicación, que definen la estructura y reglas de los documentos en la base de datos.

Por ejemplo: un modelo `User` que define los campos y validaciones para los usuarios, como `nombre`, `email`, `contraseña`, etc.
